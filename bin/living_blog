#!/usr/bin/env ruby
# frozen_string_literal: true

require 'dotenv/load'
require_relative '../lib/living_blog'

begin
  LivingBlog::Update.new.run!(dry_run: ENV['DRY_RUN'] == 'true')
rescue LivingBlog::Error => e
  puts "[living-blog] #{e.message}"
end
